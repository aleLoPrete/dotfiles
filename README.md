# DotFiles

Configuration files for

- neovim
- tmux
- ghostty

## Install

Clone repository inside ~.

```bash
git clone git@github.com:aleLoPrete/dotfiles.git 
```

Use `stow` to install configuration files

```bash
stow tmux 
stow nvim
stow ghostty
```
